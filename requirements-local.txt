# Local Development Requirements
# Simplified dependencies for offline local mode

# Core dependencies
pydantic>=2.0
dataclasses-json>=0.5.7
typing-extensions>=4.0

# Database (SQLite only for local)
sqlalchemy>=2.0

# API and web  
fastapi>=0.100
uvicorn>=0.23
httpx>=0.24

# Data processing (minimal)
pandas>=2.0
numpy<2  # For compatibility
jsonlines>=3.1

# Document processing (optional)
nltk>=3.8
# spacy>=3.5  # Optional, causes NumPy issues
pdfminer.six>=20231228

# LLM support (optional)
requests>=2.31  # For Ollama

# Utilities
tqdm>=4.65
python-dotenv>=1.0
pyyaml>=6.0
click>=8.1

# Quality metrics (basic)
scipy>=1.10
scikit-learn>=1.3

# Testing (optional)
pytest>=7.4
pytest-asyncio>=0.21

# Additional for local mode
aiofiles>=23.0  # For file operations
python-multipart>=0.0.6  # For file uploads
