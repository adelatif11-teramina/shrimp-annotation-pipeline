# Railway Configuration for Shrimp Annotation Pipeline

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "bash railway_direct_start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
healthcheckPath = "/api/health"
healthcheckTimeout = 120